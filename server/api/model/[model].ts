import { ZenStackMiddleware } from "@zenstackhq/server/express";

export default defineEventHandler(async (event) => {
  // TODO: Handle event with ZenStackMiddleware (i.e. express middleware)
  return {};
});
